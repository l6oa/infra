terraform {
  <% if (!local) { %>
  backend "s3" {
    bucket = "<%= tfstatesBucketName %>"
    key    = "<%= project %>/<%= environment %>/amplify.<%= name %>.tfstate"
    region = "<%= tfstatesBucketRegion %>"
  }
  <% } %>

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = ">= 5.82"
    }
  }
}
