terraform {
  backend "s3" {
    bucket = "<%= tfstatesBucketName %>"
    key    = "<%= project %>/<%= environment %>/efs.<%= name %>.tfstate"
    region = "<%= tfstatesBucketRegion %>"
  }

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 4.0"
    }
  }
}
